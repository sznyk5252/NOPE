#include "nope_runtime.h"

int main() {
	    if (x > 10) 	{
	        if (!(x != 100)) {
	            printf("[NOPE] Failed test on condition: CHECK(x != 100)\n");
	            return 1;
	        }
	}
	return 0;
}